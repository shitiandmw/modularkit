{
  "compilerOptions": {
    "target": "ESNext",             // 指定 ECMAScript 目标版本
    "moduleResolution": "node",     // 用于模块解析策略
    "strict": true,                 // 启用所有严格类型检查选项
    "esModuleInterop": true,        // 允许从没有默认导出的模块中默认导入
    "skipLibCheck": true,           // 跳过所有声明文件的类型检查
    "outDir": "./dist",             // 指定输出目录
    "declaration": true,            // 生成相应的 '.d.ts' 声明文件
    "sourceMap": true               // 生成相应的 '.map' 文件
  },
  "include": [
    "src/**/*"                      // 指定要编译的文件夹
  ],
  "exclude": [
    "node_modules",                 // 排除不需要编译的文件夹
  ]
}
